# 核心深度学习库
torch
torchvision
torchaudio

# 关键依赖 (版本敏感)
transformers==4.53.2
peft
accelerate

# ReconVLA / Diffusion 相关
timm
einops
tqdm
numpy
scipy

# OpenPI / JAX 兼容性依赖 (即使只用 PyTorch 也需要这些来通过 Config 检查)
blobfile
pytest
beartype
jaxtyping
jax
jaxlib
ml_collections

# 监控与工具
wandb